[project]
name = "nova-provenance-chain"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/nova-provenance-chain/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.nova-provenance-chain]
path = "contracts/nova-provenance-chain.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
